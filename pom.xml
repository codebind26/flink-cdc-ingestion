<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.learning</groupId>
    <artifactId>flink-cdc-ingestion</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>flink-cdc-ingestion</name>
    <description>CDC ingestion from PostgreSQL to Paimon lakehouse</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <flink.version>1.20.1</flink.version>
        <flink.cdc.version>3.5.0</flink.cdc.version>
        <paimon.version>1.2.0</paimon.version>
        <log4j.version>2.24.3</log4j.version>
    </properties>

    <dependencies>
        <!-- ==================== -->
        <!-- Flink CDC Pipeline   -->
        <!-- ==================== -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-cdc-cli</artifactId>
            <version>${flink.cdc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-cdc-composer</artifactId>
            <version>${flink.cdc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-cdc-common</artifactId>
            <version>${flink.cdc.version}</version>
        </dependency>

        <!-- ==================== -->
        <!-- Postgres CDC Source  -->
        <!-- ==================== -->
        <!-- Bundled into uber JAR (not a separate plugin) like the company project -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-cdc-pipeline-connector-postgres</artifactId>
            <version>${flink.cdc.version}</version>
        </dependency>

        <!-- ==================== -->
        <!-- Paimon Sink          -->
        <!-- ==================== -->
        <!-- Bundled into uber JAR (not a separate plugin) like the company project.
             The connector fat JAR bundles its own paimon classes, but we exclude them
             via shade plugin filter and use paimon-flink-1.20 instead (single copy of Options). -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-cdc-pipeline-connector-paimon</artifactId>
            <version>${flink.cdc.version}</version>
        </dependency>

        <!-- Paimon runtime - provides the SINGLE copy of org.apache.paimon.* classes.
             The company project uses paimon-bundle:1.4-SNAPSHOT (provided scope) because
             Amazon Managed Service for Apache Flink has it on the classpath.
             For local execution, we include it as compile scope so it's in our uber JAR. -->
        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-flink-1.20</artifactId>
            <version>${paimon.version}</version>
        </dependency>

        <!-- ==================== -->
        <!-- Flink Core           -->
        <!-- ==================== -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-clients</artifactId>
            <version>${flink.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-streaming-java</artifactId>
            <version>${flink.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-table-api-java-bridge</artifactId>
            <version>${flink.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-connector-base</artifactId>
            <version>${flink.version}</version>
        </dependency>

        <!-- ==================== -->
        <!-- Hadoop (needed by Paimon CatalogContext) -->
        <!-- ==================== -->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <version>3.4.1</version>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-hdfs-client</artifactId>
            <version>3.4.1</version>
        </dependency>

        <!-- ==================== -->
        <!-- Flink Web UI          -->
        <!-- ==================== -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-runtime-web</artifactId>
            <version>${flink.version}</version>
        </dependency>

        <!-- ==================== -->
        <!-- Logging              -->
        <!-- ==================== -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j-impl</artifactId>
            <version>${log4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>${log4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <minimizeJar>false</minimizeJar>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>log4j.properties</exclude>
                                        <exclude>hive-log4j2.properties</exclude>
                                    </excludes>
                                </filter>
                                <!-- KEY FIX: Exclude ALL bundled paimon classes from the connector
                                     fat JAR. We use paimon-flink-1.20 as the single source of
                                     org.apache.paimon.* classes. This prevents the ClassCastException
                                     on Options that happens when two copies exist. -->
                                <filter>
                                    <artifact>org.apache.flink:flink-cdc-pipeline-connector-paimon</artifact>
                                    <excludes>
                                        <exclude>org/apache/paimon/**</exclude>
                                        <exclude>paimon-codegen/**</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.learning.cdc.CdcIngestionApp</mainClass>
                                    <manifestEntries>
                                        <Multi-Release>true</Multi-Release>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
